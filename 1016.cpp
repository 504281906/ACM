#include <stdio.h>
int ss(int x,int y)
{
 	int f,s,j;
 	s=x+y;
 	f=1;
 	for (j=2;j<=s/2;j++)
 	 if (s % j ==0) 
 	 {f=0;break;}
    return f;
}
int main()
{
 	int ss(int x, int y);
 	while (scanf("%d",&n) && n!=0)
 	{
	 	  memset(b,1,20);
	 	  a[1]=1;k=1;b[1]=0;
	 	  while (k<=n)
	 	  {
		   	 for (i=1;i<=n;i++)
			   if (ss(a[k],i))  	
   		  }
    }
}

