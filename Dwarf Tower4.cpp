#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <algorithm>
#include <iostream>
#include <cmath>
#include <queue>
#include <map>
#include <stack>
#include <list>
#include <vector>
using namespace std;
#define LL __int64
int main()
{
	vector<int>v[100010];
	while (~scanf("%d%d",&n,&m))
	{
		memset(d,0,sizeof(d));
		for (i=1;i<=n;i++)
			scanf("%d",&a[i]);
		for (i=1;i<=m;i++)
		{
			scanf("%I64d%I64d%I64d",&k,&x,&y);
			d[k]++;
			v[x].push_
		}
	}
	return 0;
}

